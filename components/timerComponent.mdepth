load "AndroidComponent"

AndroidComponent timerComponent {
	
	AComp Timer {
		package = "be.pds.timer";
		gencodetemplate = "timer.egl";
		ctype = "timer";

		countdown : int;
		continuous : boolean;
	}
		
}
